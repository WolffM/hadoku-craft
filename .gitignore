# Dependencies
node_modules
# Local scratch / sample decks (not version-controlled)
temp/

# Python
.venv/
__pycache__/
*.pyc
*.egg-info/

# Build outputs
dist/
build/

# Environment variables
.env
.env.local
.env.*.local

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

resources/
.npmrc

# OS
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*
pnpm-debug.log*

# Testing
coverage/
.nyc_output/

# IDE
*.code-workspace

# Temporary
.tmp/
.cache/

# Agent worktrees (`git worktree add .claude/worktrees/<task>`). A worktree is
# a FULL second checkout, so without this a `git add -A` commits one whole.
# Scoped deliberately: the REST of .claude/ is tracked (2 files are here), and a
# blanket `.claude/` would silently swallow the next file added to it.
.claude/worktrees/
